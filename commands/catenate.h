/**
 * Fuction: catenate
 * --------------
 * catenate files and print to stdout
 * 
 * files : names of files to catenate.
 * argc : the number of files
 * 
 * return : 0 if non error otherwise error is occured
*/
int catenate(char **, int);